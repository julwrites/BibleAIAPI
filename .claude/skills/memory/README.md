# Memory Skill

This skill allows the agent to store and retrieve long-term information using `scripts/memory.py`.

## Tools

- `memory_create`: Create a memory.
- `memory_list`: List memories.
- `memory_read`: Read a memory.

See `docs/interop/tool_definitions.json` for schema.
